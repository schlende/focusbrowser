import * as React from "react";
import * as ReactDOM from "react-dom";

ReactDOM.render(
    <div>Hello this is a test of the emergency broadcasting system</div>,
    document.getElementById("app")
);